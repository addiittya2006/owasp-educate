<h1 align="center">Listing features</h1>
<% @features.each do |feature| %>
    <div class="jmb container-fluid col-md-4">
      <div class="jumbotron">
        <h1><%= feature.count %></h1>
        <p><%= feature.name %></p>
      </div>
    </div>
<% end %>
